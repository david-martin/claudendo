# claudendo

Nintendo 3DS homebrew that photographs a scene and speaks a robotic one-line description via Claude Haiku vision and espeak-ng.

## Relay

The Python relay lives in `relay/`. See `requirements.txt` for dependencies.

## 3DS

The 3DS homebrew app lives in `3ds/`. Requires devkitPro.
